import { makeStyles } from "@material-ui/core/";

export const useBigButton = makeStyles({
    bigButton: {
        fontWeight: 700,
        textTransform: "capitalize",
        width: "100%",
    },
});
